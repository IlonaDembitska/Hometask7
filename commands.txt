# Hometask #7 — SSH commands used in steps 2, 5, 6

# Step 2 — Dynamic port forwarding (-D)
ssh -i "IXT_key.pem" -D 8888 ubuntu@EC2_PUBLIC_IP

# Step 4 — Local web server on home computer
python -m http.server 8000

# Step 5 — Reverse SSH tunnel (-R)
ssh -i "IXT_key.pem" -R 8080:localhost:8000 ubuntu@EC2_PUBLIC_IP